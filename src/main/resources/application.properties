spring.application.name=UniRewards

# URL de Conexão
spring.datasource.url=jdbc:postgresql://db.logixvrbnxyuklwwouad.supabase.co:5432/postgres?tcpKeepAlive=true

# Usuário e Senha
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD}

# Configurações do Hibernate/JPA
#spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuração de porta (usa variável de ambiente PORT ou 8080 por padrão)
#server.port=${PORT:8080}